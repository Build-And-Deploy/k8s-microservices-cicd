// db.js
// Brendand Dileo, May 26 2025

const mongoose = require('mongoose');

const connectDB = async () => {
    try {

    } catch (err) {
        console.error('', err);
        process.exit(1);
    }
}